require 'spec_helper'

describe JackalGame do
  it 'has a version number' do
    expect(JackalGame::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
